#!/bin/bash
# CloudCannon prebuild script - SIMPLIFIED
echo "Starting CloudCannon prebuild..."

# Just verify theme exists - NO MODIFICATIONS
if [ ! -d "themes/bigspring" ]; then
    echo "ERROR: Theme 'bigspring' not found!"
    exit 1
fi

echo "Theme verified. Ready to build."